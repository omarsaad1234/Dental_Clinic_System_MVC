@model Dental_Clinic.Models.DentalHistory

@{
    ViewData["Title"] = "Details";
}
<dl class="row">
    <dt class="col-sm-2">
        @Html.DisplayNameFor(model => model.Patient)
    </dt>
    <dd class="col-sm-10">
        @Html.DisplayFor(model => model.Patient.Name)
    </dd>
</dl>

<table class="table">
    <thead>
        <tr>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>
                @for (int i = 7; i >= 0; i--)
                {
                    if (Model.Up_Left_Details[i] != null)
                    {
                        if (Model.Up_Left_IsWorkingOn[i])
                        {
                            <a asp-action="TeethDetailUpLeft" asp-route-id="@Model.Id" asp-route-num="@i"><img class="working" src="~/Up_@(i).png" data-toggle="tooltip" data-placement="top" title="@Model.Up_Left_Details[i]" /></a>
                        }
                        else
                        {
                            <a asp-action="TeethDetailUpLeft" asp-route-id="@Model.Id" asp-route-num="@i"><img class="modified" src="~/Up_@(i).png" data-toggle="tooltip" data-placement="top" title="@Model.Up_Left_Details[i]" /></a>
                        }
                    }
                    else
                    {
                        <a asp-action="TeethDetailUpLeft" asp-route-id="@Model.Id" asp-route-num="@i"><img class="plain" src="~/Up_@(i).png" /></a>
                    }


                }
                @for (int i = 0; i < 8; i++)
                {
                    if (Model.Up_Right_Details[i] != null)
                    {
                        if (Model.Down_Right_IsWorkingOn[i])
                        {
                            <a asp-action="TeethDetailUpRight" asp-route-id="@Model.Id" asp-route-num="@i"><img class="working" src="~/Up_@(i).png" data-toggle="tooltip" data-placement="top" title="@Model.Up_Right_Details[i]" /></a>
                        }
                        else
                        {
                            <a asp-action="TeethDetailUpRight" asp-route-id="@Model.Id" asp-route-num="@i"><img class="modified" src="~/Up_@(i).png" data-toggle="tooltip" data-placement="top" title="@Model.Up_Right_Details[i]" /></a>
                        }
                    }
                    else
                    {
                        <a asp-action="TeethDetailUpRight" asp-route-id="@Model.Id" asp-route-num="@i"><img class="plain" src="~/Up_@(i).png" /></a>
                    }
                }
            </td>
        </tr>
        <tr>
            <td>
                @for (int i = 7; i >= 0; i--)
                {

                    if (Model.Down_Left_Details[i] != null)
                    {
                        if (Model.Down_Left_IsWorkingOn[i])
                        {
                            <a asp-action="TeethDetailDownLeft" asp-route-id="@Model.Id" asp-route-num="@i"><img class="working" src="~/Down_@(i).png" data-toggle="tooltip" data-placement="top" title="@Model.Down_Left_Details[i]" /></a>
                        }
                        else
                        {
                            <a asp-action="TeethDetailDownLeft" asp-route-id="@Model.Id" asp-route-num="@i"><img class="modified" src="~/Down_@(i).png" data-toggle="tooltip" data-placement="top" title="@Model.Down_Left_Details[i]" /></a>
                        }
                    }
                    else
                    {
                        <a asp-action="TeethDetailDownLeft" asp-route-id="@Model.Id" asp-route-num="@i"><img class="plain" src="~/Down_@(i).png" /></a>
                    }

                }
                @for (int i = 0; i < 8; i++)
                {
                    if (Model.Down_Right_Details[i] != null)
                    {
                        if (Model.Down_Right_IsWorkingOn[i])
                        {
                            <a asp-action="TeethDetailDownRight" asp-route-id="@Model.Id" asp-route-num="@i"><img class="working" src="~/Down_@(i).png" data-toggle="tooltip" data-placement="top" title="@Model.Down_Right_Details[i]" /></a>
                        }
                        else
                        {
                            <a asp-action="TeethDetailDownRight" asp-route-id="@Model.Id" asp-route-num="@i"><img class="modified" src="~/Down_@(i).png" data-toggle="tooltip" data-placement="top" title="@Model.Down_Right_Details[i]" /></a>
                        }
                    }
                    else
                    {
                        <a asp-action="TeethDetailDownRight" asp-route-id="@Model.Id" asp-route-num="@i"><img class="plain" src="~/Down_@(i).png" /></a>
                    }
                }
            </td>
        </tr>

    </tbody>
</table>


<a asp-action="Index">Back To All Dental Histories</a>